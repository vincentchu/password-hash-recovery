{
  "name": "password-hash-recovery",
  "version": "1.0.0",
  "description": "Smart contract for recovering password hashes",
  "scripts": {
    "init": "$(npm bin)/truffle init",
    "compile": "$(npm bin)/truffle compile",
    "start": "$(npm bin)/testrpc --mnemonic='color coral ozone benefit purse model consider warrior soap host result symptom'",
    "test": "$(npm bin)/truffle test",
    "migrate": "$(npm bin)/truffle migrate"
  },
  "dependencies": {},
  "devDependencies": {
    "truffle": "^3.4.9",
    "ethereumjs-testrpc": "^4.1.1",
    "eslint": "^3.12.0",
    "eslint-config-google": "^0.7.1",
    "eslint-config-recommended": "^1.5.0"
  },
  "author": "Vincent Chu",
  "license": "MIT"
}
